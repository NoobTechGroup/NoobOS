import React from "react";
import folderIcon from "../../folder.svg";

/**
 * FIXME: Use the correct props to create a BaseIcon for Folder and files
 */

const Icon = () => {
  return (
    <div>
      <img src={folderIcon} width="48" height="48" />
    </div>
  );
};

export default Icon;
