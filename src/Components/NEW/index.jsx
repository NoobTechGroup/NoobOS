// Dependencies
import React from "react";

// Components

// Styles
import { NEWSTYLE } from "./styles";

const New = (props) => {
  return (
    <NEWSTYLE>
      <div>{props.prop}</div>
    </NEWSTYLE>
  );
};

export default New;
